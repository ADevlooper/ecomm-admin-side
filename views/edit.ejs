<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Product</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 flex justify-center items-center min-h-screen">
  <div class="bg-white p-10 rounded-xl shadow-lg w-2/3">
    <h2 class="text-3xl font-bold mb-6">Edit Product</h2>

    <form action="/update/<%= product.id %>" method="POST" enctype="multipart/form-data" class="space-y-4">

      <input type="text" name="name" value="<%= product.name %>"
        class="w-full p-3 border rounded" />

      <textarea name="description"
        class="w-full p-3 border rounded h-24"><%= product.description %></textarea>

      <input type="number" name="price" value="<%= product.price %>"
        class="w-full p-3 border rounded" />

      <input type="number" name="rating" min="1" max="5" value="<%= product.rating %>"
        class="w-full p-3 border rounded" />

      <div>
        <p class="font-semibold mb-2">Current Image:</p>
        <% if (product.image_url) { %>
          <img src="<%= product.image_url %>" class="w-32 h-32 rounded shadow mb-3" />
        <% } %>
        <input type="file" name="image" />
      </div>

      <button class="bg-yellow-600 text-white px-6 py-2 rounded hover:bg-yellow-700">
        Update Product
      </button>
    </form>
  </div>
</body>
</html>
